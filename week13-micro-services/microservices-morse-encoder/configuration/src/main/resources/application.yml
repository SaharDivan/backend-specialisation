server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SaharDivan/config-repo
management:
  endpoints:
    web:
      exposure:
        include: "*"